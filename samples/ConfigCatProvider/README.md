# OpenFeature ConfigCat SDK example

This example provides an example of bootstrapping and using the OpenFeature ConfigCat provider.

1. Install packages using [Composer](https://getcomposer.org/):
   ```
   composer install
   ```
2. Run in console:
   ```
   php main.php
   ```
